[
  "dry run exposes a 9-direction and 9-color matrix without mistaking routing for execution",
  "official design state proof blocks ingest, requires explicit retry, and rejects replay tamper",
  "coherently resealed premature complete state is rejected before resume can spawn",
  "direction and color reviewers are independent from the full creator provider union",
  "design provider roles reject KillSlopRouter parent aliases before state creation",
  "design result actors cannot impersonate the KillSlopRouter parent at any stage",
  "design start rejects an in-memory host declaration forgery before child intent",
  "design resume rejects coherent brief, baseline, and state-directory authority reseals before child spawn",
  "design state rejects phase-less shortlist and approval injection with immutable sources",
  "digest-bound reference reasoning reaches creators without changing the exact-three matrix",
  "metadata-only research pack blocks design before state or child creation",
  "reference pack cannot grant authority or cross the project boundary",
  "reference pack physical identity and grammar-to-reasoning edges remain fail closed",
  "component craft specifications survive direction/color browser review, Owner gates and final resume",
  "component craft blocks missing component specification before browser spawn",
  "component craft blocks mismatched recipe digest before browser spawn",
  "component craft requires three viewports at preflight without changing the ordinary two-viewport route",
  "bound reference reasoning requires creator traces and independent design checks",
  "bound reference reasoning remains mandatory through color review",
  "reference-backed design closes trace, evidence, copy, network, and producer gaps",
  "reference-backed reviewers require bounded source authority before spawn",
  "authorized read-only reference captures reach only the independent design critic",
  "source-privileged reference participants cannot reappear as design creators or browser actors",
  "source-recipient executable authority cannot be aliased by design creators or browser providers",
  "a failed historical source executable remains forbidden after retry on a different graph",
  "partial capability and missing browser adapters remain manual_pending",
  "design CLI creates resumable state, reports status, and dispatches exact packets",
  "resumable exploration crosses real child processes, owner gates, and compiles verified receipts",
  "final approval compilation rejects evidence changed after the owner checkpoint",
  "final approval publish revalidates evidence and removes untrusted staging output",
  "creator self-review and unofficial browser evidence fail closed",
  "byte-identical matrices, repeated palettes, and low distinctiveness fail closed",
  "font license evidence and implementation token mismatches fail closed",
  "execution failures resume only through an explicit retry selector",
  "overlapping design resume is rejected before a second child starts",
  "design CLI inspects and recovers a stale child lease before explicit retry",
  "post-child design checkpoint recovery does not rerun an accepted child",
  "fault-injected design state writes retain a recoverable exclusive lease",
  "pending and published final approval writes recover without duplicate publication",
  "resealed design packet, result, evidence, and accepted-attempt lineage tamper fails closed",
  "per-read authority caching revalidates nested module graphs at its final boundary",
  "contrast ratios are recomputed locally instead of trusting adapter claims"
]
