interface:
  display_name: "KillSlopRouter"
  short_description: "Bootstrap and run fail-closed anti-slop audits"
  default_prompt: "Use $kill-slop-router to bootstrap this project, run its independent evidence-bound audit, and resume every required gate to an exact final status."
