interface:
  display_name: "KillSlopRouter"
  short_description: "Bind exact visual signatures and route fail-closed reviews"
  default_prompt: "Use $kill-slop-router to bind this artifact to its semantic surface, verified visual intent, and exact visual signature, then continue the fail-closed audit."
