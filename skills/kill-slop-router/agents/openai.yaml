interface:
  display_name: "KillSlopRouter"
  short_description: "Route anti-slop reviews by verified visual intent"
  default_prompt: "Use $kill-slop-router to bind this artifact to its semantic surface and verified visual intent, then continue the fail-closed audit."
