interface:
  display_name: "KillSlopRouter"
  short_description: "Route anti-slop reviews by locked product surface"
  default_prompt: "Use $kill-slop-router to lock this project artifact to its semantic product surface, continue the safest eligible audit stage, and report exact receipts or pending evidence."
