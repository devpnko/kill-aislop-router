interface:
  display_name: "KillSlopRouter"
  short_description: "Route and verify independent anti-slop reviews"
  default_prompt: "Use $kill-slop-router to classify this artifact, select one creator, dispatch independent critics, and finalize an evidence-bound audit receipt."
